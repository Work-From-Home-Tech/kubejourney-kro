apiVersion: kro.run/v1alpha1
kind: WordpressServer
metadata:
  name: wordpress-prod
  namespace: production
spec:
  name: wordpress-prod
  namespace: production
  ingress:
    enabled: true
    host: wp-production
  db_password: "prod-secret-password"
  replicas: 2
